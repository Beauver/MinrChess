@namespace beauver_chess_global
    ChessPiece makePiece(String piece, String color)
    Void resetBoardArray() 
    Int coordinateToIndex(Int x, Int y)
    Int minecraftCoordinateToIndex(Int x, Int z)
    String pieceToHeadString(ChessPiece piece)

    BlockLocation boardTopLeft
    BlockLocation boardTopRight
    BlockLocation boardBottomLeft
    BlockLocation boardBottomRight

    ChessPiece[] board
    String turn
    Boolean firstClick
    Int indexFirstClick
    ChessPiece selectedPiece
    Block selectedBlock

    @class ChessPiece
       String piece
       String color
    @endclass

@endnamespace